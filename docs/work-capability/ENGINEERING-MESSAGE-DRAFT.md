Draft only; not sent. Suggested location: INF-177 discussion, addressed to Seungho/Homin. Source/status details are in ENGINEERING-REVIEW.md; refresh the issue discussion before posting if time has passed.

Seungho, Homin: the local ChatGPT Work experiment now passes a native two-turn capture test, including quitting and reopening the app. It preserved both prompts and all 20 tool call/result pairs through the existing Codex queue and shared preprocessor. I also fixed the expiry-retention and read-only credential issues, plus a resume bug found when the transcript file was replaced. The replacement fix passes deterministic tests; the latest app-restart test didn't replace the file. Full Mac restart validation is deferred, and I've removed the temporary plugin and purged its queue.

I'd like to get your review of the bounded Work changes before adding upload support. Could we agree on the per-task consent flow and duration, and which shared credential contract this should consume under INF-177/INF-200 and #37/#38? For the eventual report test, we also need an approved environment/account, its dashboard-user mapping, and confirmation that the analyzer is appropriate for that Work sample.

The source and evidence packet are ready locally. The Work branches haven't been pushed, and this hasn't reached production upload, actual analysis or the dashboard. I can keep the Work delta separate from the existing Codex repair review.
