{
  "name": "skillmeter-codex-marketplace",
  "private": true,
  "description": "SkillMeter telemetry plugin marketplace for Codex. The plugin version is the single source of truth in plugins/skillmeter/.codex-plugin/plugin.json.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SkillBench-AI/skillmeter-codex-marketplace.git"
  },
  "scripts": {
    "test": "node --test",
    "check:version": "node .github/scripts/check-version.mjs",
    "check:manifests": "node .github/scripts/validate-manifests.mjs",
    "check": "npm run check:version && npm run check:manifests && npm run check:compatibility && npm test",
    "check:compatibility": "node .github/scripts/compatibility-contract.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
